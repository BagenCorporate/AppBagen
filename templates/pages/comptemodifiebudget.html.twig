{% extends "layouts/base.html.twig" %}

{% block stylesheets %}
    <link href="http://localhost/bagen/style/tableau_depense.css" rel="stylesheet">
{% endblock %}

{% block body %}
   <div>
            <form  method="POST" action="{{ path('compte.valider.budget', {id:compte.id}) }}" >
                <div class="form-group">
                    <div class="col-md-12">
                        <div>
                            <label>Budget : </label>
                        </div>
                        <div>
                            <input type="text" name="montant" value="{{ compte.lebudget }}" autofocus >
                        </div>
                        <div>
                            <button "type="submit"  >Valider
                            </button>
                        </div>
                    </div>
                </div>  
            </form>
        </div>
{% endblock %}

